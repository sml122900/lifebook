# 2026-06-25 작업 일지 — 장소 1:N (사건에 여러 장소)

## 배경

한 인생 사건(UserMemory)에 장소를 **단 1곳**만 붙일 수 있었다(평면 5컬럼 `placeName/placeAddress/lat/lng/placeSource`). 부모님 요청 — "한 사건에 여러 장소"(예: 신혼여행지 여러 곳, 직장이 옮겨다닌 여러 사무실). 1:1 → 1:N 전환을 H1~H5 단계로 나눠 안전하게(롤백·호환 보존) 진행.

전환 원칙: **추가만, 제거는 나중(H6)**. 기존 5컬럼은 호환·롤백용으로 H5까지 유지. 각 단계 `tsc --noEmit` 통과 확인.

## H1 — 스키마 + 백필 (마이그 2건, 성민이 migrate deploy)

- 새 모델 `MemoryPlace` (id/memoryId FK Cascade·index/placeName(NOT NULL)/placeAddress/lat/lng/placeSource/sortOrder/createdAt). `UserMemory.places MemoryPlace[]` 역참조 1줄.
- 마이그 `20260624000000_memory_place_table` — CREATE TABLE.
- 마이그 `20260624000001_backfill_memory_place` — 기존 UserMemory 중 placeName 있는 행 → MemoryPlace 1건씩(sortOrder=0). **멱등**: `NOT EXISTS` 가드로 재실행 안전, `gen_random_uuid()::text` id.
- ★ 5컬럼 삭제 X. `prisma migrate deploy` 적용 완료(성민이 실행, 44 마이그).

## H2 — 서버 읽기 (places[] 반환)

- `lib/life-events.ts` 3개 읽기 함수(`getLifeEvents`·`getLifeEventForCategory`·`getLifeEventById`): select에 `places` 관계 include(sortOrder ASC), 반환 타입에 `places: PlaceInfo[]` 추가. 단일 `place`도 당분간 유지.
- `lib/photos.ts` `listUserPhotos`·`listMemoryPhotos`: `UserPhoto.places[]` 추가(memory.places join).
- `lib/people.ts` `listEventsByPerson`(수동 LifeEvent 매핑): tsc 강제로 places select+map 추가.
- 빈 배열 = 장소 없음. 단일 place 소비처(TimelineView 등)는 무수정 → 동작 변화 0.

## H3 — 서버 쓰기 (MemoryPlace 기록)

- `LifeRecordInput.places?` 추가. 헬퍼 `normalizePlaces`(places[] 우선·없으면 [place] 흡수·`validatePlace`로 거른 valid + 5컬럼용 primary).
- `createLifeEvent`/`upsertLifeEvent`(create): `userMemory.create`에 중첩 `places: { create }`(암묵 트랜잭션).
- `updateLifeEvent`/`upsertLifeEvent`(update): `$transaction([update, deleteMany, createMany])` — 기존 싹 지우고 재생성(원자적·부분실패 전체 롤백).
- `lib/photos.ts` `buildPhotoMemoryData`에 중첩 places, `updatePhotoMemoryPlace`(나중 H5에서 배열화) 트랜잭션.
- ★ 5컬럼은 `primary`로 계속 write(호환).
- companion 추출은 UserMemory에 place 안 씀(장소=별도 `Person subjectType=location`) → 무관. 온보딩 사건화·G2는 `createLifeEvent` place 없이 호출 → 자동 처리.
- **보안 픽스**: `updatePhotoMemoryPlace` 트랜잭션화 시 MemoryPlace엔 userId 없어 deleteMany가 소유 검증 불가 → 남의 memoryId로 피해자 장소 삭제 가능. 소유 확인을 트랜잭션 *앞*에 두어 차단(updateLifeEvent `current` 패턴과 동일).

## H4 — UI 표시 (배열 렌더)

- `TimelineView.tsx` `PlacePreview`: 단일 → `places[]` 순회. placeName 있는 것만, **세로 한 줄씩 칩 나열**(어르신 친화 구분). 단일 칩은 `PlaceChip`로 추출(externalMapHref source 분기 유지). 빈 배열 → `return null`(빈 핀 X).
- 호출부 3곳(`e.place`→`e.places`) + `PhotoCard` `localPlace`→`localPlaces`.
- `poster/page.tsx`: 출생지 라벨 `birth.places.find(p=>p.placeName)`.
- `DraftReview`/`DraftLocationCard`는 Person(location) draft 입력이라 무관, people/[id]·/photos는 place 미표시 → 변경 불필요.

## H5 — 다중 입력 UI (핵심)

- **새 `app/components/PlacesEditor.tsx`** — `value: PlaceInfo[]`/`onChange` 배열 관리. 한 개 추가는 기존 `PlaceSearchInput` 재사용(선택 완료 시 append, "선택 안 함"=취소). 고른 장소 카드 + 큰 [제거] 버튼, "장소 추가"/"장소 더 추가"/"추가 취소".
- `EventForm`·`CategoryForm`: place 단일 state → `places[]`, PlaceSearchInput → PlacesEditor.
- `actions.ts`(life-timeline/life-record): `places?` 입력 추가, validate가 각 항목 검증 후 헬퍼에 `places` 전달. 단일 `place?`는 호환 유지.
- `PhotoPlaceModal`: 단일 → 다중(기본 0~1). `updatePhotoMemoryPlace` → 배열형 `updatePhotoMemoryPlaces`, `updatePhotoPlaceAction(memoryId, rawPlaces[])`.
- 업로드 폼(PhotosUploadForm)은 단일 그대로(업로드 시 1곳이 흔함).
- prefill: 편집 진입 시 sortOrder 순 places[] → 카드 N개. 0개 저장 OK / 다중 저장·재편집 / 제거 정상.
- F5 `PlaceableMultiItemWidget`(온보딩 LifeProfile Json 저장)은 별개 흐름이라 무수정.

## 미완료 후속

- **H6**: 단일 place 5컬럼 제거(마이그 DROP COLUMN) + `LifeEvent.place`·`getLifeEvent*` 단일 필드 제거 + normalizePlaces의 `[place]` 호환 분기 제거. 일정 기간 5컬럼·MemoryPlace 공존 안정 확인 후.
- 기존 단일 장소 편집 UX(PlaceSearchInput의 (C)단계 "다른 곳으로 바꾸기")는 PlacesEditor에선 "제거 후 재추가"로 단순화 — 개별 장소 인라인 편집은 후속(어르신엔 제거+추가가 더 단순).
- 가족 룸(`listRoomMemories`)·인물 화면은 아직 장소 미표시 — 1:N 노출 여부 후속.
- `db/test-photo-place.ts`는 새 배열 시그니처로 갱신했으나 prod Supabase 의존(실행은 성민이 판단). `MemoryPlace` 전용 회귀 스크립트(생성·백필 멱등·다중 저장·삭제) 후속 후보.
- DB 검증 스크립트들이 `place` 단일 인자로 LifeRecordInput 구성 — normalizePlaces 호환 분기로 통과하지만, H6에서 호환 제거 시 함께 갱신 필요.

## 커밋

| 커밋 해시 | 내용 |
|---|---|
| (작성 시 갱신) | feat(place): 한 사건에 여러 장소 — MemoryPlace 1:N (스키마·백필·읽기·쓰기·표시·다중 입력 H1~H5) |

---

# 2026-06-25 세션2 — AI 추출 품질 픽스 (P0 검증 + S1~S6)

## 배경

외할머니(이유옥) 인터뷰 9개(0618 3개 + 0624 6개)를 실서비스 추출 함수(`splitRecordingTranscript` + `extractPeopleFromTranscript`, Sonnet)로 돌려 품질을 실측(P0). 7개 결함 발견 → S1~S6 단계별 픽스 + 각 단계 P0 재검증.

검증 스크립트 `scripts/extract-test.ts`(읽기 전용·DB write 0·gitignore). 대상 계정 = `sml122900@naver.com`(이유옥).

## P0에서 발견한 결함
- 화자 매핑이 녹음일별로 다름(0618-1·2는 참석자1=외할머니, 나머지는 참석자2) — task 전제와 반대
- "쓰지 마라"한 거절 구혼자(9남매 의사 집안) 신상이 사건+인물로 박제됨 (프라이버시)
- DB 기존 11건과 거의 전부 중복 재생성 (split/extract가 기존 연혁을 안 받음)
- 자식 마일스톤(콜롬비아대 진학 등)이 할머니 사건으로 혼입
- 긴 전사(0618-2: 19,306자) → 12,000자 컷 + maxTokens 2048 초과로 JSON 깨져 사건 0건
- User.birthYear 컬럼 null(BIRTH 이벤트엔 1935) → 연도추정 불가
- 취향(색·꽃)이 사건화됨

## S1 — 프라이버시 리댁션 전처리 (`lib/transcript-redact.ts` 신규)
- 추출 LLM이 거부 구간을 *아예 못 보게* 전처리에서 결정적 제거(프롬프트 차단=확률적이라 불충분).
- 트리거 = 키워드 코드 매칭("쓰지 마라/빼줘/적지 마/넣지 마/비밀" 등, "빼고" 오탐 제외). 윈도우 = 트리거 ±(앞4·뒤2) 문장 고정 → 100% 재현. 마스킹 토큰 치환.
- ★ 추출 입력에만 적용. 원본 transcript·audio 보존(부모 메모리 그대로).
- 한계(surface): 트리거에서 멀리(36문장) 자유 서술된 블록은 못 잡음 → 다층 방어로 흡수.

## S2 — 중복 회피 (스킵 + 새것만)
- 사건: 프롬프트 주입(1차, 기존 사건 목록 보여주고 회피) + 코드 dedup(2차, 보수적 — 연도 ±1 AND 의미 토큰 ≥3글자 일치, 조사 제거 후 비교). "결혼식 vs 결혼여행"·"미국 파견 vs 이민" 오스킵 방지.
- 인물: 프롬프트 주입은 *과억제*(친척 목록 보여주면 새 인물까지 전부 0개로) 실측 → **코드 dedup**(이름 정규화 매칭)으로 전환.
- parseResponse: 빈 segments(=전부 중복)도 정상 처리(원문 덤프 fallback 분리, JSON 깨질 때만 fallback).

## S3 — 자식 마일스톤 가드 (split 프롬프트 규칙 0-3)
- 주어(행위 주체)가 어르신 본인일 때만 사건화. 자식 독립사건(콜롬비아대 진학·기숙사 입소)은 사건 X, 인물 정보로만. 단 "내가 애들 데리고 미국행"·"대학원 등록"·"출산"은 어르신 사건으로 보존.

## S4 — 긴 전사 청크 처리
- `splitOneChunk` 헬퍼 추출, 단일(≤12,000자)/청크 분기. `chunkByUnits`(턴 경계 + 끝 2턴 overlap) → 청크별 split → `mergeSplitResults`(경계 중복 보수적 제거). maxTokens 2048→4096.
- 0618-2: 0건 → 6~7건(짝사랑 정우영·천재 오빠·1953 콩쿠르·정원모 교수 등). companion·온보딩 양쪽 시그니처 무변경(내부 리팩터).

## S5 — birthYear 파생/fallback (`resolveBirthYear`)
- 우선순위: User.birthYear 컬럼 → 없으면 BIRTH life_event 연도 → null. `fetchCompanionProfile`·companion/free-record 추출 경로 모두 적용.
- 컬럼 null에서도 BIRTH 이벤트서 1935 파생 → 연도추정 동작 확인. (성민이 실DB User.birthYear=1935 수동 UPDATE 완료.)

## S6 — 취향 분리 + 이름 단정 완화
- S6-a: 취향(색·꽃·음식·계절·성격)은 사건 X → `SplitResult.preferences[]` 별도 반환(포스터 디자인용, 저장·UI는 후속 P3~P5). 청크 병합 시 중복 제거.
- S6-b: extract 프롬프트 — 이름 불확실하면 단정 X, 관계를 name으로("남편"·"사촌 언니"), 관계어("고종사촌")를 인물명으로 변형 금지. → "고종원" 환각 해소·어머니 "이름 불명확" 표시.

## 최종 재검증 (S1~S6 합본 3런)
- 정답지 9개 마일스톤(1935 출생~1990 선상여행) 전부 커버(DB 중복 deduped + 신규 추출).
- 6대 체크 3런 모두 통과: S1 구혼자 상세 0 / S2 DB 명백중복 0 / S3 자식 독립사건 0(인물 memo만) / S4 0618-2 6~7건 / S5 1935 / S6 취향 분리.
- ⚠️ 수용한 soft 잔존(3런 일관): 어머니 인물 memo "부잣집 의사 혼처 선호" 한 줄 — 거부 핵심 신상은 전부 제거됨, 비식별 약한 언급만. 다층 방어(draft·검토·주문전 확인)로 흡수.

## 미완료 후속
- S1 잔여(트리거서 먼 거부 서술)·S6-b 잔여(자신있는 이름 오인, 0624-1 남편 "김하은")은 다층 방어 영역.
- 크루즈 여행 등 cross-session/cross-file 중복은 실서비스에선 S2 기존-사건 주입으로 잡힘(검증 스크립트는 9파일 독립 추출이라 표시됨).
- preferences 저장 위치·UI(P3~P5 포스터 디자인 활용).

## 커밋
| 커밋 해시 | 내용 |
|---|---|
| (작성 시 갱신) | feat(extraction): 회상 추출 품질 픽스 S1~S6 (리댁션·중복회피·자식가드·청크·birthYear·취향분리) |

---

# 2026-06-25 세션3 — 포스터 파이프라인 완성 (P1~P7-b + 동선)

## 배경
연혁 → 인생 나무 포스터를 "고르기 → 노드·메모 선택 → 편집 → 주문·발주·환불"까지 풀스택 완성.

## P1~P4 (선별·문장·선택 UI·SVG 합성)
- **P3** `lib/poster/poster-sentences.ts` `refineForPoster`(Sonnet 고정) — nodeLabel(명사 짧게)·memoText(1인칭 회상체).
- **P1** `lib/poster/poster-candidates.ts` `selectPosterCandidates` — node(인생분기)/memo(자잘) 분류·우선순위·추천. 무스키마.
- **P2** `Poster` 모델(userId @unique, selections Json) + `/poster/select` 3택(노드/메모/제외)·메모 상한 20·정정·미리보기. 마이그 `20260625000000_poster`.
- **P4** `lib/poster/compose-layout.ts`(순수 28/28) + `PosterCompose.tsx`(클라: 폰트 로드·getBBox 실측·canvas luminance 적응형 톤·SVG 렌더) + `/poster/view`. 배경 `public/poster/river-bg.png`.
- ⚠️ `"use server"` 값 export 빌드깨짐 → `MAX_MEMO_ITEMS` 순수 모듈 `lib/poster/select-constants.ts` 분리.
- 노드 박스 패딩 축소(상하 비대칭 `NODE_TOP_TRIM`) — 시제품처럼 글자에 타이트.

## /poster 동선 재생 + 템플릿 선택
- 옛 직접편집기 `_PosterEditorArchived` 보존, `/poster` = 템플릿 카드(강물 실작동 / 느티·인생나무·맞춤형 "준비 중"). `chooseTemplate` → Poster.template.
- 동선: 연혁 → /poster → select → view → order. select "다음: 시안 보기", view "주문하기"·"템플릿 바꾸기".

## P6 액티브 편집
- `/poster/view` 노드·메모 글상자 편집(드래그·A± 크기·연도/제목/문장·빼기). per-item override(`lib/poster/overrides.ts`) — 없으면 자동배치. `savePosterOverrides`. 안전여백(노드150/메모34) 밖 경고.

## P7-a 인쇄 export
- Canvas 2D로 model 재렌더 → 5008×7063 @300dpi PNG. SVG 직렬화·CJK 폰트 임베드 없이(로드된 Noto fillText). 세로 crop 131/132. **pHYs 청크로 300dpi 메타**(CRC32 IEND 검증). `app/poster/print-export.ts`.

## P7-b 주문·발주·환불 (실결제 OFF)
- 기존 `ProductOrder`/Toss/`settleProductOrder` 재사용. `/poster/order`(재질 일반39k/프리미엄99k·배송·결제). `POSTER_PAYMENT_LIVE_ENABLED` 기본 OFF.
- 발주 = 관리자 페이지 `/admin/orders`(ADMIN_EMAILS 3중 게이트). 주문 시점 `posterSnapshot`으로 P7-a export·상태변경·송장. `OrderFulfiller` 어댑터(ManualFulfiller no-op).
- 환불 = 발주 전(paid)만. 사용자 `/account/orders` 환불요청 → 관리자 `refunded`. 마이그 `20260625100000`.

## 커밋
| 커밋 해시 | 내용 |
|---|---|
| f163f15·dd95722 | P1·P3 |
| e1954ae·26edd0a | P2+P4·배경 |
| f7b1c1e | "use server" 값 export 픽스 |
| 8c781c4·fcc0e2a·f2f8245·a62c370 | 노드 패딩 축소 |
| e1b039b | /poster 템플릿 선택 + 동선 |
| 0c25965 | P6 액티브 편집 |
| d1b26e4 | P7-a 인쇄 export |
| 92c4b62·54da730 | P7-b 주문·발주·환불 + 주문 CTA |

---

# 2026-06-25 세션4 — 프로덕션 정리 + AI 모델 선택

## 프로덕션 정리 (테스트→실사용 1단계)
- 전수 조사(빌드X) 후 "지금 할 것"만: 콘솔로그 NODE_ENV 게이트(`[ai]`·`[ai:web_search]`·`[tokens]`는 userId PII 제거), opus-4-7 → **opus-4-8**(+ `supportsTemperature` 가드를 opus-4 패밀리로), `_test_archived` 제거.
- 유지: 플래그 3종 OFF·가격 39k/99k·archived 라우트. 출시 직전 사안(ADMIN_EMAILS·통신판매업·시행일·결제flag)은 보류.
- 커밋 `23ec2d0`.

## AI 모델 선택 (전역 라이브 모델)
- **단일 소스** `lib/ai-model.ts`(타입·ID·배수·라벨·경고) + `lib/user-ai-model.ts`(get/set, `cache()`). `User.aiModel`(기본 haiku, 마이그 `20260625110000`).
- **라이브 = 전역 모델 + 배율**: companion(신규 ×배율 차감)·refine·assistant ask(depth→전역 1:1)·tutorial(opus→sonnet 클램프·무료). 배율 통일 **{1,3,8}**(assistant opus 5→8).
- **추출 = Sonnet 고정**(opus 차단): companion 추출·통녹음 분할·온보딩 파싱(haiku→sonnet)·사건화·인물·포스터 P1/P3. (검증: 추출부 getUserAiModel 참조 0)
- **UI**: `AiModelChips`(full=설정 / compact=라이브) → `updateAiModel` 전역 갱신. 설정 페이지 섹션(opus 8배 경고) + companion 칩. **기존 토글 제거**: refine tier·assistant depth·온보딩 model.
- 결정: companion 유료·tutorial 무료(클램프) — 핵심 대화는 모델·비용 일관, 도움말은 공짜.

## 미완료 후속
- assistant 화면 칩(부모 5곳 prop threading 회피로 안내문만) — 필요 시 추가.
- 출시 직전: ADMIN_EMAILS·통신판매업 신고번호·/privacy 시행일·결제 라이브 flag.

## 커밋
| 커밋 해시 | 내용 |
|---|---|
| 23ec2d0 | chore: 프로덕션 정리(콘솔로그 게이트·opus 4.8·아카이브 제거) |
| (작성 시 갱신) | feat(ai): 전역 AI 모델 선택(라이브 haiku/sonnet/opus·추출 sonnet 고정·배율 차감) |
