# 2026-06-19 작업 일지

## 완료한 작업

### Phase 8 — Person.subjectType (이야기 주체 확장)

이야기 주체를 인물("person") 외 장소("location")·물건("thing") 으로 확장.
geo 장소(UserMemory.placeName/lat/lng)와 완전히 별개 테이블/컬럼.

**스키마·마이그레이션**
- `Person.subjectType String @default("person")` ADD COLUMN
- `@@index([userId, subjectType])` 탭 쿼리 최적화
- `prisma/migrations/20260619000003_person_subject_type/migration.sql` 생성·적용

**lib/people.ts**
- `SUBJECT_TYPE = { PERSON, LOCATION, THING }` 상수 + `SubjectType` 타입
- `isSubjectType()` 검증 헬퍼
- `PersonInput`/`Person` 타입에 `subjectType` 추가
- `PERSON_SELECT` const 공용 select 객체로 중복 제거
- `validatePersonInput` — isPerson 분기(location/thing은 relation·birthYear·metYear·category 무시)
- `createPerson`/`updatePerson`/`listPeople`/`getPerson` 모두 subjectType 포함
- `listPeopleByEventBatch` 반환 타입에 `subjectType: SubjectType` 추가

**app/people/actions.ts**
- `PersonInputRaw`에 `subjectType` 추가
- `normalize()` — "location"|"thing" 외는 모두 "person" 강제(안전 폴백)

**app/people/PersonForm.tsx**
- `subjectType` prop 추가 (기본값 "person" → 기존 호출부 회귀 0)
- `SUBJECT_LABELS` — 라벨/플레이스홀더 타입별 분기
- `isPerson` 분기로 category·relation·birthYear·metYear 섹션 `{isPerson && (...)}` 래핑
- `buildRaw()` — location/thing은 person 전용 필드 null 강제(defense-in-depth)

**app/people/page.tsx (전체 재작성)**
- 인물/장소/물건 3탭 UI — URL `?tab=` searchParam (SSR, JS 없이 탭 전환)
- 탭별 카운트 표시 + 활성 탭 스타일
- person 탭 = 카테고리 그룹핑 유지, location/thing = 단순 목록
- 탭별 "추가" 버튼이 `/people/new?type=<tab>` 링크

**app/people/new/page.tsx**
- `?type=location|thing` 수신 → `subjectType` prop 전달
- 제목·힌트 문구 타입별 분기 (`SUBJECT_TITLE`/`SUBJECT_HINT`)
- 뒤로 버튼이 `/people?tab=<subjectType>` 로

**app/people/[personId]/page.tsx**
- `SUBJECT_TYPE.PERSON` 아닌 경우 category·relation·birthYear·metYear 섹션 숨김
- 연결 섹션 제목 분기("함께한 인생의 순간들" vs "등장한 이야기들")
- 뒤로 버튼 → `/people?tab=${person.subjectType}`

**app/people/[personId]/edit/page.tsx**
- 제목 분기 ("인물 수정" / "장소 수정" / "물건 수정")
- `person.subjectType === "person"` 조건으로 birthYear fetch 분기

**app/life-timeline/PeopleConnectModal.tsx**
- `PersonLite = { id, name, subjectType }` 타입 확장
- `PersonRow` — 이름 앞 📍(location) / 📦(thing) 아이콘

**app/life-timeline/TimelineView.tsx**
- `PeopleByEvent = Record<string, PersonLite[]>` 타입 통일 (기존 `{id,name}[]` → `PersonLite[]`)

**app/life-timeline/page.tsx**
- `allPeople` 매핑에 `subjectType` 포함
- `peopleByEvent` 타입 `SubjectType` 기반

**db/test-people.ts**
- 모든 `createPerson`/`updatePerson` 호출에 `subjectType: "person"` 추가 (tsc 정합)

### STEP 0 조사 (9번 지도 업그레이드 — 코드 변경 X)

Google Places Autocomplete (`places:autocomplete`) API 테스트 결과:
- `GOOGLE_MAPS_API_KEY`에 HTTP referrer 제한 → 서버 사이드 fetch 403 (`API_KEY_HTTP_REFERRER_BLOCKED`)
- 제한 없애도 `places:autocomplete` 엔드포인트 자체가 `{}` 반환 (GCP 프로젝트에서 미활성화 추정)
- **결론**: GCP Console에서 (1) 키 제한 "없음"으로 변경, (2) Places API v2 autocomplete 활성화 후 STEP 2 가능

### Prisma 마이그레이션 3건 적용

- `20260619000001_user_memory_audio_path`
- `20260619000002_user_consent_version`
- `20260619000003_person_subject_type`

## 검증

- `tsc --noEmit` 오류 0
- `next build` 성공
- `prisma migrate deploy` 3건 적용 완료

## 다음

- Google Places Autocomplete STEP 2 (GCP 키 설정 후)
- `/people/link` 페이지 — location/thing 인물 연결 UX (현재 동일 PersonEvent 재사용)
- `sidePanel` 진입점 "인물록" → 탭 기본 person (현재 OK)
- era_event 회상(EraMemoryEditor) 장소/물건 주체 연결 여부 (정책 미결)
