# 2026-09-07 — v3 P14 (Chrome prod 검증 test20 픽스)

## P14-1. 짧은 답변 시 요약이 봇 안내문으로 저장됨 (최우선, 데이터 유실)

**원인 2겹.**
1. `submitEpisodeTurn` 의 P10-3 처리가 종료 신호가 든 *메시지 전체* 를 요약
   입력에서 뺐다. "역사 수업 시간에 재밌는 이야기를 많이 해주셨어요. 그걸로
   된 것 같아요" 처럼 한 메시지에 내용+종료가 섞이면 내용까지 통째로 사라져
   1턴 대화가 "본인 발화 0" 이 됐다.
2. 그 상태로 요약 모델(Sonnet)을 부르면 "내용 부족" 판단 → 사용자에게 더
   말하라는 2인칭 안내문을 생성 → `finishEpisodeChat` 이 빈 문자열만 아니면
   그대로 `Episode.content` 로 저장했다.

**수정.**
- `lib/episode-text.ts` 신설(순수 모듈, 클라·서버·검증 공유) —
  `EXIT_PHRASES`/`EPISODE_DONE_PHRASES` 와 `isExitIntent`/`isEpisodeDoneIntent`
  를 ChatV3Client 에서 이곳으로 이동. `stripEpisodeDoneSignal(text)` 가 문장
  단위(마침표·물음표·느낌표·줄바꿈)로 나눠 종료 신호가 든 문장만 버리고
  내용을 돌려준다. `isSummaryGuidance(text)` 가 요약 결과의 2인칭 안내
  패턴(주세요·주시면·드리겠·습니다·정리할 내용·내용이 없·~신가요·물음표
  끝)을 판정.
- `ChatV3Client.submitEpisodeTurn` — 종료 신호 감지 시 남는 내용이 있으면
  마지막 본인 턴으로 transcript 에 넣고 마무리. `handleSend` 명시 종료
  ("그만할래요") 경로도 동일.
- `finishEpisodeChat` — (a) 본인 발화 0 이면 모델을 부르지 않고
  `저장할 이야기가 없어요` 반환("이 대화에는 실제 이야기 내용이 없어…"
  판정문 저장 차단), (b) 요약 결과가 안내문이면 거부 → 더 강한 지시를 붙여
  1회 재시도 → 그래도 안내문이면 본인 발화 원문 폴백 저장. 어떤 경로로도
  안내문은 content 에 못 들어간다.
- `EPISODE_SUMMARY_SYSTEM_PROMPT` — "절대 사용자에게 말 걸지 말 것", "입력이
  한 문장이어도 -다체 서술로 정리" + 예시 2개(핸드오프 사례 그대로).
- `finishEpisodeStage` — 본인 턴 0 이면 `saveEpisode` 를 안 타고(“저장하지
  못했어요” 노출 방지) 컨텍스트만 비운 뒤 "다른 이야기도 있으세요?".

## P14-2. 동명이인 역할 무시 병합

- `lib/person-relation.ts` 신설 — `relationGroup`(teacher/senior/junior/peer/
  spouse/parent/…) + `isRelationCompatible(a, b)`. 정책: 어느 한쪽이 비었거나
  "지인"(추출 기본값) → 병합 / 문구 동일·포함 → 병합 / 둘 다 부류가 잡히면
  같은 부류만 병합 / 그 외(부류 다름, 미분류 불일치) → 별도. 애매하면 병합
  안 함(오병합은 잘못된 존칭까지 이어짐).
- `saveOrLinkPerson` dedup 조건에 `isRelationCompatible(existing.relation,
  candidate.relation)` 추가. "국사 선생님 박정호"(고교) / "동아리 선배
  박정호"(대학) → 별도 Person, 두 번째는 "박정호 선배님과".

## P14-3. "집사람" 호칭

- "집사람 분 이야기군요" 는 코드 문구가 아니라 에피소드 대화·open 모드
  LLM 응답. `buildPersonAddress` 는 이미 집사람→아내분 매핑이 있었음.
  → 에피소드 챗 프롬프트·open-chat 분류 프롬프트에 "배우자 지칭어는
  아내분/남편분으로 바꿔 부르라" 규칙 추가, 요약 프롬프트에도 "아내/남편으로
  쓰라" 추가. `SPOUSE_TERMS` 에 안사람·안식구·영감 보강.

## P14-4. 마무리 멘트 단조로움

- `EPISODE_CLOSINGS` 4종을 순서대로 돌려 씀(연속 중복 0). 모델이 이미
  마무리 말을 한 경우(afterModelClosing)는 기존대로 "다른 이야기도 있으세요?".

## P14-5. 소프트 종료 후 갭 진입 시 간헐 빈 화면 (조사만)

- 클라 `init()` 은 서버 액션 실패를 catch → `enterError("시작하지
  못했어요.", init)` 로 **재시도 버튼이 이미 있다**. 단 이는 페이지가
  마운트된 *뒤* 의 액션 실패에만 해당.
- 관찰된 "RSC GET 503" 은 `/story-review → /chat-v3` 소프트 내비게이션의
  RSC payload 요청 자체가 실패한 것 — ChatV3Client 가 마운트되기 전이라
  init 도, `app/error.tsx`(렌더 throw 경계)도 개입하지 못한다. Next 는 이 경우
  하드 내비게이션 폴백을 시도하는데 그것마저 503 이면 빈 화면. 코드 레벨
  재시도 지점이 없고, 원인은 서버(Vercel 함수 콜드스타트/과부하) 쪽으로
  보인다 — 백로그 "간헐 503" 과 같은 계열로 두고 Vercel 함수 로그에서
  503 발생 시각·함수를 먼저 확인할 것. 이번 범위에선 변경 0.

## 검증

- `npx tsc --noEmit` 통과, eslint(변경 파일) 0.
- `db/test-p14.ts` 신규 35건 ALL PASS — 종료 신호 분리 7 · 안내문 가드 7 ·
  관계 호환 8 · 배우자 호칭 4 · DB dedup 시나리오 7(테스트 계정
  `deleteAccountTx` 로 삭제 확인). 외부 API 호출 0.
- `db/test-p11.ts` 회귀 — 1건 FAIL 은 **pre-existing**(“사장 → 격식 조사”가
  `김영희 사장과` 를 기대하나 P12-4 NIM_TITLES 이후 `사장님과` 가 정답 —
  stale 기대값, 이번 변경과 무관. 나머지 전부 OK).
- dev 종료 확인 후 `rm -rf .next && npm run build` 통과.

## 후속

- `db/test-p11.ts` "사장" 기대값을 P12-4 동작(`사장님과`)으로 갱신.
- 요약 안내문 거부 시 `console.warn` 만 남김 — 빈도 텔레메트리 후 프롬프트
  보강 여부 결정.
- `isRelationCompatible` 미분류 관계("옆집 아저씨" vs "이웃")는 별도 처리됨 —
  실사용에서 과분리 사례 나오면 어휘 보강.
