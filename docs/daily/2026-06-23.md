# 2026-06-23 작업 일지

## 완료한 작업

### 채팅 온보딩 Step E — 인물록 추가 (전 세션에서 이어짐)

6-22 세션에서 컨텍스트 초과로 중단된 Step E를 오늘 마무리.

#### `app/onboarding-chat/actions.ts` 추가
- `PersonCandidate { relation: string; name: string | null }` 타입
- `extractOnboardingPeople(answers)` — siblings·parentsInfo·closeFriends 텍스트 조합 → Sonnet으로 인물 후보 JSON 추출(`PEOPLE_EXTRACT_MODEL` = companion-extraction.ts 와 동일 env). 실패 시 `[]` 반환(흐름 막지 않음). 정제: relation 없으면 제외, name 빈 문자열 → null 정규화, 최대 5명 cap
- `saveOnboardingPerson(name, relation)` — `createPerson(userId, { subjectType:"person", ... })`. `isDraft` 기본값(`false`)으로 즉시 확정. companion AI 추론(isDraft=true) 경로와 완전 분리

#### `app/onboarding-chat/OnboardingChatClient.tsx` 변경
- `Phase` 타입에 `"people"` 추가
- `candidates: PersonCandidate[]` / `candidateIdx: number` state 추가
- `saveAndComplete` → `saveAndGoPeople` 교체:
  - profile 저장 → `extractOnboardingPeople` → 후보 ≥1이면 `phase="people"`, 없으면 `/life-timeline` 직행
- `askCandidate(idx, cands)` — 첫 후보엔 안내 문구("말씀하신 분들을 인물록에...") + 성함 질문, 이후 후보는 질문만
- `handlePeopleInput(input)` — raw input이 이름 (LLM 파싱 0). isSkip이면 건너뜀, 아니면 `saveOnboardingPerson` 후 다음 후보로. 마지막 후보 완료 후 `/life-timeline` 이동
- `handleSubmit` 진입부에 `phase === "people"` 분기 추가
- `showInput` 에 `"people"` 포함 / `renderWidget` people 페이즈에서 null / placeholder "성함을 입력하세요…" / 버튼 "이 분은 넘어가기 →"

#### 검증
- `tsc --noEmit` 오류 0
- 커밋: `148ba07 feat(onboarding): Step E — 온보딩 끝 인물록 추가 채팅`

---

## 미완료 후속

- 온보딩 채팅 → 인물 추출 텔레메트리 — 후보 평균 N명, 스킵률, 추출 실패율
- 온보딩 → companion 연결 검증 — 온보딩에서 확정된 인물이 `buildSystemPrompt` [어르신 프로파일]에 정상 반영되는지 실사용 테스트 (isDraft=false 필터 이미 적용됨)
- 온보딩 인물 + companion draft 중복 케이스 — v1 범위 외(dedup은 나중 /manage 병합). 중복 발생 시 /people 에서 수동 정리
- `extractOnboardingPeople` 프롬프트 실측 — "선생님 한 분" 같은 경계 케이스 제외 정확도 확인 후 예시 보강 여부

---

## 커밋 목록

| 커밋 해시 | 내용 |
|---|---|
| 148ba07 | feat(onboarding): Step E — 온보딩 끝 인물록 추가 채팅 |
