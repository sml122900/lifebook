# 2026-09-05 — v3 P13 긴급 (Chrome prod test20: 딥링크 진입 후 세션 고착)

## 근본 원인 (P13-1·P13-2 공통)

P12-6 에서 딥링크 소비 직후 `window.history.replaceState(null, "", "/chat-v3")` 로 주소만 정리하려 한 것이 원인. Next 16.2.6 앱 라우터는 `replaceState` 를 패치해 `ACTION_RESTORE` 를 dispatch 하고(`app-router.js` `applyUrlFromHistoryPushReplace`), `restoreReducer` 가 세그먼트 캐시로 `startPPRNavigation` + `spawnDynamicRequests` 를 돌린다. 페이지 세그먼트 키에 searchParams 가 포함되므로(`__PAGE__?{gapEventId…}` → `__PAGE__`) 페이지 서브트리가 **리마운트**됐다 — 새 인스턴스는 `initialGap=null` 로 init 을 다시 돌고, 첫 인스턴스의 init 체인은 고아가 된다. test20 DB 로그가 이를 그대로 보여줌: 딥링크 세션마다 안내/오프닝 버블이 0건, 한 세션은 stage 초기값(`profile_year`)의 "태어나신 연도" 폴백으로 응답. 주소 변경이 없는 open 모드만 정상이던 이유. 사용자가 본 `{"f":"","q":"","i":false}` 는 이 restore 가 띄운 RSC 요청 응답. "소프트 종료"는 무관한 선행 조건이었다(서버에 closed 상태 없음).

## 픽스

- **P13-1** — `replaceState` 제거. 대신 stale 딥링크 표식: 채팅 안 갭 칩 클릭 시 `sessionStorage["chat-v3:deep-link-consumed"]=<eventId:kind:personId>`, init 은 같은 키의 initialGap 을 무시(pending/자연 분기). 클라이언트 라우팅 언마운트 시 표식 제거(같은 카드 재클릭은 새 의도), 새로고침은 언마운트를 안 타 표식 유지. 주소는 절대 건드리지 않는다.
- **P13-2** — pending 과 다른 딥링크는 원래 코드도 딥링크 우선이었으나(P9-2 는 "같을 때"만 resume) 리마운트 증상으로 겹쳐 보임. 명시적으로 `if (pending) clearPending()` 을 start* 앞에 추가(옛 컨텍스트 잔존 0).
- **P13-3** — `withTimeout`(45s) 로 서버 액션 9곳 래핑 → 초과 시 catch → `enterError`(다시 시도). 빈 응답 폴백 `"잠시 문제가 있었어요. 다시 말씀해주세요."`(episode·open). `finishSession` 은 `setStatus("finished")` 를 addBot await *앞*에 두어 전환 중 입력 창구 제거.
- **후속 결함(발견)** — 종료 직후 재진입 시 P9-3/P3-6 dedupe 가 "마지막 메시지가 봇 발화" 만 보고 확인질문/인사를 안 붙여 입력창만 남았다. `SESSION_END_MESSAGES` + `endsWithPendingBotTurn()` 으로 마무리 문구는 "대기 중 질문"에서 제외(3곳 통일).

## 검증 (로컬 dev + Chrome, 시드 계정 p13-test → deleteAccountTx 로 삭제)

- person 딥링크 → 인물 저장 → 소프트 종료 → period 딥링크(pending 과 다름) → **봇 정상 응답** → 종료 → episode 저장 + 이순자 등록 → 3회차 episode 딥링크 → 완곡 종료 → 칩(결혼 이후) → **같은 딥링크 URL 새로고침 → 칩 대화 복원**(stale 무시) → 3회차 소프트 종료 저장. 소프트 종료 3회 후 갭 진입 정상.
- 골격 완료 전환: 답변 직후 입력 disabled → `/story-review` 이동.
- 빈 응답: `respondToOpenChat` 을 임시로 `""` 반환시켜 폴백 문구 노출 확인 후 원복(diff 0).
- 종료 후 재진입: "하고 싶은 이야기 있으세요?" 인사 정상 부착.
- `tsc`/eslint 0, dev 종료 후 `rm -rf .next && npm run build` 통과, `db/test-person-mode.ts` 회귀 통과.
- test20: `ChatV3PendingContext` 0건(이미 없음) + `OnboardingChatMessage` 74건 삭제. LifeEvent·Episode·Person 보존 → /story-review 갭 카드부터 재진입 가능.

## P13-4 open 모드 저장 없음 — 조사·제안

- 현황: `respondToOpenChat` 은 단일 턴 `chat()` 호출뿐 — 히스토리·요약·저장·인물 추출 전부 없음(설계 의도: "짧게 듣고 반응만"). 사용자가 여기서 실질 이야기를 하면 로그(OnboardingChatMessage)에만 남고 Episode/Person 으로는 안 간다.
- 판단: 유실이 맞다. 어르신은 "하고 싶은 이야기 있으세요?" 에 실제 이야기를 꺼내는 게 자연스럽고, 갭 카드가 3장 미만인 후반부일수록 open 모드 체류가 길어진다.
- 제안(작업 안 함): (a) 최소 — open 턴에서도 이름 있는 인물만 `savePeopleMentionedInEpisode` 와 같은 추출을 돌려 Person 저장(연결 이벤트는 없으니 PersonLifeEvent 없이, 또는 최근 이벤트에 연결). (b) 권장 — open 발화가 "실질 이야기"(길이 ≥ N자 또는 연도·사건 언급)면 episode 엔진으로 승격: `CUSTOM` LifeEvent(제목=요약 1줄) 를 만들고 STAGE4 로 이어가 마무리 시 Episode 저장. (c) 대안 — open 모드 진입 시 "말씀하신 이야기는 따로 저장되지 않아요. 저장하려면 아래 카드를 골라주세요" 안내. 우선순위는 (b) > (a) > (c), 결정 필요.

## P13-4(b) 구현 — open 모드 실질 이야기 승격 (세션 2)

- **분류** — `lib/open-chat-promote.ts` `classifyOpenTurn(text, birthYear)`: Sonnet 1회 호출로 `{story, title, year}` 또는 `{story:false, reply}` 를 함께 받는다(비승격 응답을 별도 호출로 만들지 않아 지연 1회분 절약). 정책: 살아온 일·사람·장소·시절이면 true, 인사·안부·짧은 반응·서비스 질문이면 false, **애매하면 true**(사용자 결정: 유실보다 과저장). 연도는 발화의 나이·학년 표현을 birthYear(OnboardingProfile → User 순)로 환산("스무 살 때" + 1950년생 → 1970), 없으면 null. title 은 명사형 ≤15자(파싱 실패 시 발화 앞 30자).
- **CUSTOM LifeEvent** — `createCustomLifeEvent`: 처음부터 CONFIRMED(본인이 꺼낸 이야기라 확인질문 불필요), isOptional. 자리(sequenceOrder)는 **연도 있으면 그 이하 연도의 마지막 이벤트 바로 뒤**(뒤 이벤트 +1 밀기 — 타임라인·갭·확인질문 조회가 전부 sequenceOrder 순이라 여기서 연도순 자리를 잡아야 /story-review 에 제자리로 보인다), **연도 없으면 맨 뒤**("시기 미상 섹션" 대신 맨 뒤로 결정 — 골격의 첫 직장/결혼 같은 연도 null 이벤트와 표시 규칙이 같고 새 UI 0).
- **승격 동선** — `respondToOpenChat` 반환이 `{kind:"reply"} | {kind:"promoted", eventId, label, year}` 로 바뀜. 클라(`submitOpenChat`)는 promoted 면 stage=episode + refs 초기화 + `markPendingEpisode` + 딥링크 소비 표식 후, 방금 발화를 첫 본인 턴으로 `submitEpisodeTurnToModel` 에 바로 넘긴다(오프닝 재질문 없이 후속 질문부터). 마무리·저장·인물 추출은 STAGE4 기존 경로 그대로(`finishEpisodeChat` → Episode + `savePeopleMentionedInEpisode`).
- **갭 감지** — CUSTOM 도 CONFIRMED 라 person/episode 갭 대상(코드 변경 0). 저장 후엔 hasEpisode=true 로 episode 갭 소멸, 인물이 연결되면 person 갭 대신 person_episode 갭.
- **검증** — `db/test-open-promote.ts`(신규): 인사 → 비승격, "스무 살 때 서울로…" → 승격 + year 1970, 자리(1963 < 1970 < 1985, null 은 맨 뒤, sequenceOrder 충돌 0), CUSTOM person/episode 갭 → 저장 후 episode 갭 소멸, story-review 순서. 브라우저 E2E(로컬 dev, p13 시드 → 삭제): "고마워요, 잘 지내요" → 반응만 / "스물다섯 살 때 부산에서 친구 김영수랑 고기잡이 배를…" → 후속 질문 → 1턴 → 완곡 종료 → /story-review 에 "1975년 부산 고기잡이 배와 높은 파도"(이야기 있음·👤 김영수)가 1963·1985 사이에, 들려주신 이야기 요약, "김영수랑 있었던 일도" 갭 카드. tsc/eslint 0, 클린 빌드 통과.

## 후속

- 승격 시 Sonnet 분류 1회 + 에피소드 응답 1회 = 어르신 체감 5~8초. 분류 응답에 첫 후속 질문까지 함께 받아 1회로 줄이는 최적화 후보.
- CUSTOM 이벤트 삭제/편집 UI 없음(과저장 정책의 전제 "나중에 사용자가 지울 수 있다"가 아직 v3 화면엔 없음 — /story-review 또는 /manage 에 CUSTOM 삭제 진입로 후속).
- 비승격 reply 가 가끔 "-습니다"체(프롬프트에 금지 명시했으나 모델 편차) — 사용자 보고 시 재조정.
- `SESSION_END_MESSAGES` 는 문구 상수 일치에 의존 — finishSession 문구를 바꾸면 이 Set 도 같이. 문구를 한 곳(상수 객체)으로 묶는 정리 후보.
- `withTimeout` 45s 는 Sonnet 추출+요약(finishEpisodeChat: 요약 + 인물 추출 2회 호출)에 여유 있으나, 긴 전사에서 초과 시 "다시 시도" 가 재저장을 유발할 수 있음(중복 Episode) — idempotency 후속.
- dev.log 의 `JWTSessionError`(PrismaClientKnownRequestError) 는 삭제된 계정의 stale 세션 쿠키 — 로그인 전 첫 요청에서만 발생, 기능 영향 0.
