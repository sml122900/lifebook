# 2026-05-28

## 한 줄 요약

타임머신 v2 비서를 V3(멀티턴+저장) → 자체 검토 픽스 → V4(답의 깊이
Haiku/Sonnet/Opus) 로 확장하고, **출석체크**(매일 5토큰 + 7일 보너스
30토큰) + **사이드 패널**(프로필·잔액·메뉴)로 동기부여·접근성까지 한 번에.

## 1. V3 — 멀티턴 대화 + 답변 저장

### 멀티턴 (컨텍스트 답 우선)

기존 비서는 질문→답 단발. v3 핵심: 후속 질문은 **검색 안 하고 이전 답을
풀어 설명**하는 게 default. 토큰 절약 + 빠름.

`askAssistant(userId, q, year, month, prior?)`:
- `prior` 가 비어있지 않으면 → 컨텍스트 답 분기
- `chat(messages, system: contextSystemPrompt)` — web_search 없이 일반 호출
- 모델이 정확히 `[SEARCH]` 한 단어를 뱉으면 → 검색 폴백
- 그 외 → 컨텍스트 답 채택 (보통 1토큰 차감)

길이 가드:
- `MAX_PRIOR_TURNS = 8` (최근 8턴)
- `MAX_PRIOR_TEXT_CHARS = 600` (각 텍스트 cap)

컨텍스트 system prompt 가 처음엔 너무 보수적이라 거의 모든 후속에서
SEARCH 던짐 → 폴백 후 DB 분기 다시 들어가 같은 답이 두 번. 두 가지 보강:
1. 프롬프트 보강 — "이전 답에 등장한 항목을 풀어 설명하는 것은 환영.
   정말 새 사실이 필요할 때만 SEARCH"
2. 폴백 정책 — `[SEARCH]` 시 DB 건너뛰고 바로 검색 (DB 는 첫 답에 이미 줬음)

실측: "1번 자세히" → 컨텍스트 답으로 자연스럽게 풀어 설명, 1토큰 차감.

### 답변 저장

새 `createdVia="timemachine_assistant"` UserMemory 행 + content 에
AnswerSnapshot 의 JSON.

T6 와 분리:
- `saveTimemachineMonth` 의 `deleteMany` 필터는 `[event, month]` 만 →
  비서 행 절대 안 지워짐
- Phase 7 `ai_chat` 도 createdVia 가 달라 분리

UI: 탭 토글 `[채팅] [저장된 답변]`. 저장된 답은 재조회 시 토큰 0 으로
재렌더.

### 검증 (`db/test-assistant-v3.ts`)

| 시나리오 | 결과 |
|---|---|
| (a) BIG/DB → 후속 "1번 자세히" | source=context, 1토큰, 검색 없음 |
| (b) 저장 → 재조회 토큰 0 | ✓ |
| (c) T6 keptEvent + 비서 저장 공존 | createdVia 3종 각자 보존 |
| (d) Phase 7 ai_chat 무영향 | ✓ |

## 2. v2 비서 자체 검토 + 픽스 6건

7개 카테고리(버그·토큰·회귀·가드·프라이버시·보안·일관성)로 자체 검토.
**높음 2 / 중간 11 / 낮음 다수**. 사용자 선택으로 6건만 픽스:

| ID | 픽스 |
|---|---|
| **B1** | `ask()` fetch 실패 시 직전 user turn 롤백 (안 그러면 prior 가 user→user 연속이 되어 Anthropic 400) |
| **B2** | `clampPrior` 후 맨 앞이 assistant 면 한 칸 더 자름 (Anthropic 은 첫 메시지 user 필수) |
| **B3** | 빈 답에 차감 X — 컨텍스트/검색 양쪽 다. 음성 다듬기 H2 와 같은 원칙 |
| **T2** | [SEARCH] 폴백에서 컨텍스트 미스 차감 후 검색 실패/빈 답 시 환불. 새 `refundTokens(userId, n, reason, refId)` 헬퍼 — wallet `balance + n` 은 음수 불가라 race-safe |
| **S1** | citation url 을 `new URL()` 파싱 + `protocol in (http, https)` 검증 후 링크. 그 외는 `<span>` 텍스트 (javascript: scheme XSS 가드) |
| **P1** | "저장하면 가족 룸 멤버에게도 보여요" 안내 — 채팅 저장 버튼 옆 + 저장 패널 상단. 결정: visibility 토글 안 만들고 항상 공유 |

미픽스 (다음에 묶음): T1, B4~B7, C1, S2, S4, K1~K5.

### 검색 답 환각 줄이기 — system prompt 강화

검색용 system prompt 에 "**검색 결과를 다루는 원칙**" 섹션 4조항 추가:
1. 결과 빈약하면 인정 — 억지로 채우지 않기
2. 검색 결과에 명확히 등장하는 사실만 단정 + **일반 상식/사전 학습으로 빈칸 메우지 말 것**
3. 미확정 내용은 답 안에 "확실하지 않다" 표시
4. 모르면 "정확한 자료를 못 찾았어요" — 추측보다 낫다

기존 가드(가사·기사 복제, 단정 톤, 이미지·임베드, 시니어 친화) 모두 유지.
모델은 그대로 Haiku.

## 3. V4 — 답의 깊이 (3단 모델 라우팅)

사용자 라벨: **간단히 / 자세히 / 가장 정확하게** (모델 이름 절대 노출 X).
검색 답에만 적용. DB 답은 무료 유지.

| depth | model | base × multiplier | 검색 답 평균 |
|---|---|---|---|
| simple | claude-haiku-4-5 | 1× | 10토큰 |
| detailed | claude-sonnet-4-6 | 3× | 30토큰 |
| precise | claude-opus-4-7 | 5× | 50토큰 |

### 토큰 정책 — 호출부 surcharge 로 차이 흡수

기존 `tokensFromUsage` + `chargeOneShot.surcharge` 무변경. 호출부에서:
```ts
const base = tokensFromUsage(in, out);
const surcharge = base * (MULTIPLIER[depth] - 1) + WEB_SEARCH_SURCHARGE;
chargeOneShot(userId, in, out, `..._${depth}`, undefined, surcharge);
```

Haiku 일 땐 `multiplier=1` → `surcharge = 0 + 1 = 1` → 기존 정책과 동일
(회귀 0). Sonnet/Opus 만 추가 가산.

### Opus 4.7 — temperature 거부

`temperature is deprecated for this model` 400 에러. Opus 4.7 은 reasoning
모델이라 temperature 안 받음. `lib/ai.ts` 에 `supportsTemperature(model)`
가드 → Opus 면 조건부 미전송.

### UI

깊이 토글 (라디오 3개) 상단. 각 옵션에 추정 토큰 미리 표시 ("약 10토큰"
"약 30토큰" "약 50토큰"). 답 카드에 `[간단히 답]` / `[자세히 답]` 배지.
후속 대화는 같은 깊이 자동 이어감 (state).

### 검증 (`db/test-assistant-depth.ts`)

| 시나리오 | 결과 |
|---|---|
| (a) 3 깊이가 각각 다른 모델 호출 | ledger 에 `_simple`/`_detailed`/`_precise` 분리, 콘솔 model= 로그로 확인 |
| (b) 토큰 비례 차등 | simple=10, detailed=43, precise=56 (실측 in 토큰 차이로 ratio 1.3x — multiplier 1.67x 미만은 검색 비결정성) |
| (c) DB 답 무료 유지 | 3 깊이 모두 tokensSpent=0 |
| (d) 컨텍스트도 이어감 | simple=1, precise=5 (정확히 1×, 5×) |

## 4. 출석체크 (시각 개편 포함)

### 데이터 모델 — UserAttendance

```prisma
model UserAttendance {
  id         String @id @default(cuid())
  userId     String
  date       String  // KST "YYYY-MM-DD"
  streak     Int     // 이 날 출석 후 누적 연속일
  bonusToken Int     @default(0)  // 0 또는 30
  createdAt  DateTime @default(now())
  @@unique([userId, date])
}
```

`wallet`/`TokenTransaction` 무변경. 새 ledger reason 두 종.

### KST 처리

```ts
function kstDateString(d = new Date()) {
  return new Date(d.getTime() + 9 * 3600 * 1000).toISOString().slice(0, 10);
}
```

서버 시간 무관, timezone 라이브러리 의존 0.

### Race-safe — `@@unique([userId, date])` 가 단일 결정자

```
$transaction {
  UserAttendance.create   ← P2002 위반 가능
  wallet.balance += credit
  TokenTransaction.create × 1 또는 2
}
catch (code === 'P2002') → "이미 출석" 분기
```

동시 두 요청에도 하나만 성공, 나머지는 친화적 안내.

### 정책

- 매일 5토큰
- 7의 배수 streak (7, 14, 21…) 마다 +30 보너스 (계속 누적 정책)
- 끊기면 streak=1 리셋
- 비난·압박 표현 0 — "오늘도 와주셨네요"

### 시각 (AttendanceCard) — 동그라미 7개

```
출석체크                        3일 연속 출석중
 ✓   ✓   ✓   4   5   6   7
amber amber amber zinc zinc zinc zinc        +30
          이번 사이클 3/7일
┌─ 이번 사이클 보상 ─────────────┐
│ 매일 출석           5토큰       │
│ 7일 연속 보너스    +30토큰      │
│ 지금까지 받은 토큰  15토큰       │
└────────────────────────────────┘
[ 오늘 출석체크하기 (5토큰) ]
```

streak=6 일 땐 "오늘 출석체크하기 (5토큰 + 보너스 30토큰!)" 예고. 받은
후엔 회색 비활성 "오늘 출석 완료!".

사이드 패널 미니: 16px 작은 동그라미 7개 + N/7 텍스트.

### 검증 (`db/test-attendance.ts`)

(a) 같은 날 두 번 / (b)(c) 7일 연속 + 보너스 / (d) 동시 요청 race-safe /
(e) 거른 후 리셋 / (f) 14일째 또 보너스 / (g) 기존 chargeOneShot 무영향
— **모두 통과**.

## 5. 사이드 패널 (프로필·잔액·출석·메뉴)

`/timemachine` 전 화면 공통 — `app/timemachine/layout.tsx` (RSC) 가 데이터
fetch 후 `SidePanelLayout` (client) 으로 wrap.

레이아웃:
- **데스크톱 (lg+)**: fixed right-0, 320px. open 시 main 콘텐츠 `lg:pr-80`
  으로 우측 양보. closed 시 main 풀폭.
- **모바일**: 평소 닫힘. `≡ 내 정보` 버튼으로 슬라이드 인 + 백드롭.
- **상태 기억**: `localStorage["timemachine-sidebar"]` = `open`/`closed`.
  첫 방문 = 열림 (시니어가 "여기 내 정보" 인지).
- SSR=open / mount 후 localStorage 적용 + `!transition-none` 으로 첫
  paint 깜빡임 차단.

콘텐츠 (위→아래):
1. 프로필 (이미지 또는 첫 글자 동그라미)
2. 토큰 잔액
3. 충전 버튼 (amber-700) → /billing
4. 출석 미니 (동그라미 7개 + 받기 버튼)
5. 빠른 이동 — 이번 달 타임머신 / 내 기록 / 가족 룸 / 회원정보 / 설정
6. 로그아웃 (zinc-500 작게, NextAuth v5 signOut server action)

데이터 재사용: `auth()`, `getBalance()`, `getAttendanceStatus()`. 새 API 0.

## 6. 채팅 UI 정비

- **고정 높이 + 자동 스크롤** — `max-h-[60vh] overflow-y-auto` + `threadRef`
  + `useEffect([messages])` → commit 후 `scrollTop = scrollHeight`. 스크롤바
  두께 12px (시니어 친화).
- **입력 박스 → 패널 최하단** (일반 채팅방 패턴): thread → 칩 → 에러 → 입력.
- **칩에 source hint** — 보통 우리 자료(emerald) / 인터넷 검색(zinc) 2줄 박스.
- **답 카드 상단 배지** — `[우리 자료]`(emerald) / `[인터넷 검색]`(zinc) /
  `[이전 답에서]`(zinc) + `[간단히 답]` 등 depth 배지. 채팅·저장 양쪽.
- **안내 어휘 일관화** — "big 이벤트" → "노래·큰 사건" / "검증된 자료" → "우리 자료".

## 7. 트러블슈팅

### Prisma 7 dev cache (UserAttendance 추가 후)

`prisma generate` 후 dev 서버가 옛 클라이언트 캐시 →
`prisma.userAttendance.findUnique` 가 undefined. 에러 페이지 표시 (`/timemachine`).
검증 스크립트는 새 프로세스라 정상.

해결: TaskStop 으로 npm 죽임 → Windows 에서 자식 next dev (PID 24500)
살아남아 포트 3000 점유 → `taskkill /PID 24500 /F` 로 직접 종료 → 새로
`npm run dev`. 사용자가 `!` 로 띄운 background bash 도 Claude 의 TaskStop
대상.

### Opus 4.7 temperature deprecated

V4 검증 중 "temperature is deprecated for this model" 400. reasoning 모델
특성. `lib/ai.ts` 에 prefix 체크 가드 추가.

## 8. 후속 (이번 세션 안 함)

v2 비서 자체 검토 미픽스 (다음에 묶음):
- T1: [SEARCH] 폴백 시 이중 차감을 UI 에 더 명확히
- B4: 후속 질문 category echo
- B5: [SEARCH] sentinel 회피성 변형 후처리
- B6: 동일 답 중복 저장 (UNIQUE 또는 idempotency key)
- B7: 저장 버튼 빠른 더블클릭 옵티미스틱
- C1: 컨텍스트 답 외부 지식 누설 모니터링
- S2: prior text 길이 cap (API 레이어)
- S4: 502 응답 메시지 마스킹
- K1: console 로그 NODE_ENV gate (v1 부터)
- K2~K5: 다크모드 추가 대응, 에러 메시지 일관성, BIG_KEYWORDS 중복

v4 신규 후속:
- 검색 토큰 추정값(10/30/50) 실측 보고 재조정
- depth 별 ledger reason 분리됨 — 분석 대시보드 검토
- 출석체크 streak 길어졌을 때 (≥30) UX 분기

기획·시드:
- 시드 범위 확장 (현재 12개월 → 더 과거로) — 사용자 보통 사용 패턴이
  검색 폴백으로 빠지는 비율 측정 후 결정

## 9. PAR 소재 (`docs/par-materials.md` 에 추가)

1. **멀티턴 컨텍스트 답 우선 라우팅** — "후속은 이전 답 안에서 답하기"
   의 sentinel 기반 폴백
2. **V2 자체 검토 7-카테고리 진단 + 6건 픽스** — 코드 변경 전 진단 →
   사용자 선택 → 우선순위 픽스
3. **`refundTokens` 헬퍼** — wallet 동일 트랜잭션 패턴 재사용, 차감
   ledger 그대로 두고 + 환불 ledger 별도 — 감사 가능 + race-safe
4. **3단 모델 라우팅** — surcharge 파라미터로 차이 흡수, 호출부 변경
   최소, 기존 정책 함수 무수정 (회귀 0)
5. **출석체크 P2002 race-safe** — `@@unique([userId, date])` 가 단일 결정자,
   동시 요청·재시도 모두 한 번만 적립
6. **사이드 패널 RSC + client wrapper 패턴** — 데이터는 server, open
   state + localStorage + main 콘텐츠 padding 토글은 client

---

# 2026-05-28 (세션 2) — 동기부여 핵심 루프 ①②

## 한 줄 요약

`phase/동기부여_핵심루프_기획.md` 의 핵심 루프를 구현. **① 쌓이는 재미**
(채운 달·기록량·12개월 진척 시각화) + **② 가족 반응**(감정 스탬프 +
"새 소식" 양방향 알림). 그 후 동기부여 코드 자체 검토 → M1/M2/M4/L6 픽스.

## 1. ① 쌓이는 재미 — 진척 시각화

기존 T6 저장(`UserMemory` 의 `timemachine_event`/`timemachine_month`) 을
**읽기 전용 집계**로 시각화. 새 모델 0.

- `lib/timemachine-progress.ts`
  - `getTimemachineProgress(userId)` — 채운 달 수 / 사건 수 / 쓴 글자 수 +
    12개월 셀(filled/eventCount/hasStory). 시드 범위(2025.6~2026.5) 최신→과거.
  - `getFilledMonthKeys(userId)` — 월 화면 배지용 distinct (year,month).
- `ProgressCard.tsx` (서버 컴포넌트) — "지금까지 N개월의 이야기를…" +
  사건/글자 칩 + 12개월 그리드(채움 amber / 빈 칸 연한 회색, 각 달 링크) +
  가벼운 이정표(첫/5/10개월). **0개월은 압박 대신 초대 문구**.
- 노출: `/timemachine` 메인(출석 카드 아래), 사이드 "내 기록" 메뉴 →
  `/timemachine`, 월 화면 prev/next 에 "기록 있음" 배지(채운 달만, 긍정).

검증 (`db/test-timemachine-progress.ts`) 14/14 — 채운/빈 달 구분(ai_chat·
manual 제외), 채운 달 3·사건 3·글자 12 정확, filledKeys 정확.

## 2. ② 가족 반응 — 감정 스탬프 + 새 소식

### 새 모델 2개

```prisma
model MemoryReaction {   // 감정 스탬프 (Comment 와 같은 polymorphic)
  roomId; targetType "user_memory"; targetId; authorId; stamp; createdAt
  @@unique([roomId, targetType, targetId, authorId, stamp])  // 룸별 (M1)
}
model FamilyFeedSeen {   // 읽음 추적 (사용자당 1행)
  userId @unique; reactionsSeenAt; recordsSeenAt; updatedAt
}
```

### 스탬프 (`lib/reactions-policy.ts` / `lib/reactions.ts`)

4종 ❤️뭉클해요 / 😊기억나요 / 👏대단해요 / 🙏고마워요. `setReaction` 은
멤버십 + 대상 가시성 가드(댓글과 동일) 후 create/deleteMany. 토글은 클라가
의도(active)를 보내고 서버는 idempotent — **race-safe(create P2002 무시 /
deleteMany count0 무시)**. `StampBar` (client) 옵티미스틱 토글, 룸
`PersonalMemoryCard` 에 얹음(기존 댓글 그대로).

### 읽음 추적 (`lib/family-news.ts`)

- "새것" = 활동 `createdAt > seenAt`. `FamilyFeedSeen` 은 첫 접근 시 `now()`
  기준선으로 생성 → 가입 전 과거 활동 소급 폭주 방지.
- `getFamilyNews(userId)` — A. 내 기록에 달린 새 반응(스탬프+댓글) /
  B. 가족(같은 룸 다른 멤버)의 새 타임머신 기록(작성자·연·월 단위 묶음).
- `getFamilyNewsCount` — 사이드 배지용 경량(룸 없으면 조기 반환).
- `markReactionsSeen`/`markRecordsSeen` — 본 시점에 seen 갱신.

### UI

- `FamilyNewsCard` (`/timemachine` 메인 맨 위, 소식 있을 때만) — "○○님이
  N월 이야기에 '뭉클해요'를 남겼어요" → 그 룸으로 이동(`#m-{id}` 앵커).
  mount 시 `FamilyNewsSeen`(client)이 markSeen → 다음 접속 때 빠짐.
- 사이드 패널 "새 가족 소식 N" 카드 + 닫힌 햄버거 버튼 개수 배지.
- **0건이면 전부 숨김** (서운한 표현 없음 — 기획 원칙).

검증 (`db/test-family-reactions.ts`) 20/20 — 토글, 중복·동시 안전, 새 반응
+나비게이션, 읽으면 빠짐, 비멤버·룸 밖 대상 차단.

## 3. 동기부여 코드 자체 검토 → 픽스 4건

7관점 진단(높음 0 / 중간 4 / 낮음 10). 사용자 선택 M1·M2·M4·L6 픽스, M3 보류:

| ID | 픽스 | 이유 |
|---|---|---|
| **M1** | 스탬프 unique·deleteMany 에 `roomId` 포함 (룸별) | 저장은 roomId 없이, 조회는 roomId 로 걸러 다중 룸에서 먹통. **전역(b) 대신 룸별(a)** 선택 — 전역은 A 룸 반응이 B 룸에 노출되는 크로스룸 누수 |
| **M2** | `markSeen` 을 raw `UPDATE … = NOW()` (DB 시계) | baseline·createdAt 이 DB 시계인데 markSeen 만 Node `new Date()` → 시계 어긋나면 "봤는데 안 빠짐" |
| **M4** | `getTimemachineProgress` 를 `$queryRaw` SUM(LENGTH()) 집계 | 글자 수 세려고 회고 본문 전체를 메모리로 로드하던 것 제거 |
| **L6** | `FamilyNewsCard` 받침 따라 을/를 (`objectJosa`) | "'뭉클해요'을" → "를" |

**M3 보류**: 메인에서 가족소식·출석 중복 쿼리(layout+page). 깔끔한 제거는
React `cache()`(검증 스크립트가 비-RSC 에서 직접 호출 → 깰 위험) 또는
layout↔page 데이터 흐름 리팩터(큰 변경) 필요. 비용은 룸 있는 사용자만
부담하고 정확성 영향 없어 다음 묶음으로 이연.

마이그레이션: `add_family_reactions_feedseen` + `reaction_unique_per_room`
(unique 추가 경고로 `migrate dev` 비대화형 차단 → 수동 SQL + `migrate deploy`).

## 4. 검증 (전부 통과)

`tsc --noEmit` ✓ · `next build` ✓ · 가족 반응 20/20 · 진척 14/14 ·
출석 · T6 15/15 · 댓글 · 룸 타임라인 회귀 0.

## 5. 후속 (이번 세션 안 함)

- **M3** — 메인/월 화면 중복 쿼리 제거 (cache() 또는 layout 단일 패스)
- 동기부여 검토 미픽스 **L1~L5, L7~L10**: 옵티미스틱 순서 역전 / markSeen
  mount 즉시 / shared_memory 죽은 경로 / 멤버 탈퇴 후 반응 잔존 / 이름 없는
  가족 이메일 노출 / markSeen 무로그 / console NODE_ENV / 이모지 tofu·다크모드
  육안 미검증 / 큰 IN 리스트
- 가족 반응 다음 단계: 가벼운 음성 반응, 자녀 실제 푸시(앱 안 표시까지만 함)

## 6. PAR 소재 (`docs/par-materials.md` 에 추가)

1. **기존 데이터 읽기 집계로 ① 동기부여** — 새 모델 0, T6 UserMemory 만으로
   진척 시각화. SQL SUM(LENGTH()) 로 본문 미로드.
2. **감정 스탬프 룸별 vs 전역 — 프라이버시로 룸별 결정** — 진단에서 전역
   누수 발견 → unique 에 roomId.
3. **읽음 추적 lazy baseline + DB 시계** — 과거 소급 폭주 방지 + 시계 일관.
4. **양방향 가족 소식 한 표면** — 한 사용자가 어르신/자녀 역할 동시, 0건 숨김.
