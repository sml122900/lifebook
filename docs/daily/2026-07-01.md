# 2026-07-01 작업 일지 — 리팩토링 1~4B (데드코드·중복·레거시·사이드 패널 IA)

MVP 기능 완성 후 **첫 정리 리팩토링**. "기능 덕지덕지"를 ★ 조사 → 계획 → 위험 낮은 순으로
쪼개서 실행했다. 모든 단계 **동작 영향 0**(스텁·라벨·그룹핑·캡슐화만), 각 단계 `tsc`+`next build`
통과 후 의미 단위 커밋.

> 커밋 `952d3ad`(1단계) ~ `0f9e93a`(4-B-4) 7건 + 문서 최신화. 마이그 0, 라우트 보존(삭제 X).
> 사전 조사는 병렬 서브에이전트 3개(lib·components·의존성/죽은코드/중복)로 진행.

---

## 0. 사전 조사 (데드코드 + 사이드 패널 IA)

코드 변경 전, 두 축으로 조사만 먼저 했다.
- **데드코드** — 병렬 에이전트 3개가 `lib/`·`components/`·의존성/죽은코드/중복을 각자 grep 전수.
  근거(참조처 카운트)와 "확실/확인필요"를 구분해 보고.
- **사이드 패널 IA** — `SidePanel.tsx` 평면 10개 메뉴를 성격별로 분류 + "덕지덕지" 지점(중복 동선·
  이름 충돌) 도출. `docs/navigation-map.md` 가 이미 전 라우트를 정리해 둬 교차검증에 활용.

핵심 발견 — 레거시 섬(`/timeline`·`/onboarding`·RAG lib·`/memory`)이 서로 물려 있어,
**단순 삭제하면 가족 룸(rooms→EventCard→/memory)·가입 흐름이 깨질 수 있음** → 삭제 전 의존
관계부터 확인하기로.

## 1. 데드코드 1단계 — 미사용 파일·export 삭제 (무위험) — `952d3ad`

삭제 직전 각 심볼의 참조처 0 을 재확인(grep) 후 제거.
- **미사용 파일**: `app/components/UserMenu.tsx` (2026-06-12 구 헤더 제거로 고아, 보존 주석 없음).
- **완전 미사용 export 7건**(자기 파일서도 안 씀): `subjectJosa`·`listStoragePhotos`·`COMPANION_MODEL`·
  `ADMIN_STATUS_FLOW`·`PRICES_INCLUDE_VAT`·`MODEL_PRICING`·`tokensFromUsageForModel`(주석까지 정리).
- **stale 텍스트**: 사이드 "설정" 힌트 "다크모드"(폐기됨) → 현행으로, `side-panel-data.ts` 주석(렌더
  구조) 정정.

## 2. 데드코드 2단계 — export 캡슐화 + 중복 상수 단일출처 (저위험) — `093d1e0`

- **export 키워드 제거 11건** — 내부 전용인데 export 붙은 상수/클래스(외부 참조 0 재확인).
  `AI_MODEL_IDS`·`DEFAULT_MODEL`·`TTS_CONFIG`·`SPLIT_MODEL`·`PHOTO_PERIOD_ANCHORS`·`DEPTH_TO_TIER`·
  storage 4건·`BackgroundGenError` 등.
- **중복 상수 단일출처** — ★ 메모리4 트랩(`"use server"` 파일은 상수 export 불가) 회피해 **순수 모듈**로:
  - 신설 `lib/life-categories.ts` → `PERIOD_CATEGORIES` (life-events·EventForm 공유, 복붙 2벌 통합)
  - 신설 `lib/timeline-constants.ts` → `LATEST_YEAR/MONTH`·`APPROX_DEFAULT_MONTH` (page·AssistantWidget·
    월 화면 archived 3중 정의 통합)
  - `CREATED_VIA_EVENT/MONTH` = `timemachine-memories` export → `timemachine-progress` import
  - `rooms.ts` `"photo"` 리터럴 → 기존 `CREATED_VIA_PHOTO` 상수 (순환 import 없음 확인)

## 3. 데드코드 3단계 — 레거시 라우트 archive 스텁 (중위험, 조사 후) — `3cf2763`

레거시 라우트는 **삭제 전 의존 조사부터** 했다(레거시 섬 정리 계획).

**조사 결론:**
- `/onboarding` 은 **가입 플로우에 안 쓰임** — 신규 가입은 `/enter` → `/onboarding-chat`. `/onboarding`
  은 오직 `/timeline`(UI 진입 0 폐루프)에서만 참조. 단 `lib/onboarding/questions.ts` 는 **현행
  채팅 온보딩·회원정보와 공유** → 보존 필수.
- `/memory` 는 **죽은 게 아님** — 파일 실존·정상 작동. 404 는 유효 eventId 부재 탓. **가족 룸이
  `EventCard` 를 렌더 → `/memory/[id]` 로 링크하는 LIVE 경로** → 보존 대상(1단계 보고서 정정).
- `/timeline` 이 끌던 **RAG 체인**(`lib/triggers`·`embeddings`·`musicbrainz`·`TriggerCard`)은 archived
  함수가 참조하면 dead 아님. `EventCard`·`ListenButton` 은 가족 룸 공용 → 보존.

**실행(삭제 X, redirect 스텁 — 타임머신 `[month]`·포스터 보존 패턴):**
- `app/timeline/page.tsx` → default `redirect("/life-timeline")`, 본문 `_TimelinePageArchived` +
  `__preserve_archived_exports`(import·헬퍼 참조 유지).
- `app/onboarding/page.tsx` → default `redirect("/onboarding-chat")`, 본문 `_OnboardingPageArchived` 보존.
- 다른 코드의 `redirect("/timeline")`(memory/actions)·`revalidatePath`·`OnboardingForm push` 3곳이
  스텁으로 안전하게 흐름 확인. 되살리기는 default export 한 줄 교체.

## 4-A. 사이드 패널 성격별 5그룹 (저위험) — `a5ededf`

평면 "빠른 이동" 10개 나열 → `MenuGroup` 헤더로 **5그룹**(시각 정리만, 링크·동작 0):
📖 내 이야기 · 👨‍👩‍👧 함께 보기 · 🎁 만들기·상점 · ⚙️ 내 계정 · 🆘 도움. 콘텐츠 그룹 최상단,
계정·도움 하단. 어르신 가독성 위해 그룹 헤더 굵은 글씨 + 묶음 간 `gap-6` 여백.

## 4-B. 동선 통합 (위험 낮은 순으로 쪼갬)

- **4-B-1+2** (`eca78a5`) — 다크모드 stale 주석 2건 정정(`AttendanceCard`·`layout.tsx`) +
  🎁 만들기·상점에 **"포스터 만들기" → /poster** 추가(`Frame` 아이콘). "둘러보기 다시 보기"는
  무조건 렌더(누락 아님) 확인.
- **4-B-3** (`288741c`) — **토큰 진입로 중복 정리**. 3곳(상단 버튼·메뉴 항목·설정 카드) 중 4-A서
  추가한 메뉴 항목 제거. 잔액 옆 빠른 접근 버튼 + 설정 카드 2곳으로 통일. 라우트·충전 흐름 무변경.
- **4-B-4** (`0f9e93a`) — **AI "그 시절" 이름 충돌 해소**. 위젯 허브 "그 시절 이야기" →
  "그 시절 떠올리기"(라벨만). 사이드 "그 시절 둘러보기"(/era, 페이지 읽기)와 "둘러보기 vs 떠올리기"
  동사로 구분. companion 진입 중복(사이드 메뉴 vs 플로팅 위젯)은 *목적이 달라* 둘 다 유지.
- **4-B-5 (조사만, 실행 보류)** — 계정 통합(회원정보+설정). 조사 결과 기술적으론 합칠 수 있으나
  ① 성격이 다름(정보 수정 vs 계정 관리) ② 합치면 탈퇴·동의·결제 민감 흐름 재배선 위험 ③ 이미
  4-A 그룹화로 인접. **위험 대비 효과 낮아 "통합 안 함" 권고** → 현행 유지.

## 5. 문서 최신화

- `docs/navigation-map.md` — 사이드 5그룹 표·위젯 허브 3버튼·레거시 archive 스텁·토큰/AI 동선
  정리 반영. 헤더에 "2026-07-01 최신화".
- `CLAUDE.md` — Phase 테이블에 "리팩토링 1~4B" 행 + 열린 결정사항 4건(레거시 스텁/IA/중복 상수)
  `[x]` 추가. 보존 대상(RAG 체인·questions·/memory·EventCard)을 명시해 새 개발자 실수 방지.

---

## 검증

- 각 단계 `tsc --noEmit` + `next build` 통과(EXIT 0). 라우트 60개 유지.
- `/timeline`·`/onboarding` redirect 스텁으로 살아있음(ƒ). 가입(`/enter`→`/onboarding-chat`)·
  인증·가족 룸→/memory 흐름 무손상.
- 마이그 0, 실고객/샘플 데이터 무관, 커밋마다 무관한 `public/sample-poster-*` untracked 제외.

## 회고

- **조사 → 계획 → 위험 순 분할 실행**이 레거시 정리의 안전판이었다. 특히 3단계에서 "삭제했으면
  깨졌을" 의존(rooms→/memory, questions 공유)을 사전 조사로 잡았다.
- "삭제 X, archive 스텁" 보존 패턴 일관 적용 — 되살리기 가능성을 남겨 리스크를 낮춤.
- IA 정리는 "무리한 통합 금지" 원칙으로 과교정을 피했다(companion 둘 다 유지, 계정 통합 보류).
