# 2026-06-22 작업 일지

## 완료한 작업

### 음성 동반자 Step 7 — 지도 장소 지정 + 사진 첨부

#### 마이그레이션 1건
- `prisma/migrations/20260622000002_person_place_fields`
- `Person` 테이블에 `lat DOUBLE PRECISION`, `lng DOUBLE PRECISION`, `placeAddress TEXT`, `placeSource TEXT` 4컬럼 nullable 추가
- `location` subjectType 전용. 기존 행 무영향
- `npx prisma migrate deploy` 적용 완료

#### `DraftLocationCard` (신규 클라이언트 컴포넌트)
- `app/life-timeline/manage/DraftLocationCard.tsx`
- 장소 draft 카드에 "▼ 지도에서 위치 찾기" 토글 → `PlaceSearchInput` 인라인 (기존 컴포넌트 재사용, 네이버/구글 선택)
- 좌표 선택 후 "✓ 지도 위치 저장 후 추가" → `approveLocationWithPlaceAction` (위경도 + `isDraft=false` 원자적 저장)
- JS 없이도 "✓ 추가" form action 폴백 (점진적 향상)

#### `DraftPhotoUpload` (신규 클라이언트 컴포넌트)
- `app/life-timeline/manage/DraftPhotoUpload.tsx`
- 사건 draft 카드 내 compact 사진 첨부
- `stripGps()` → `POST /api/photos` (기존 패턴 그대로)
- `isDraft=true` 메모리에도 즉시 첨부 가능 (승인 전후 무관)
- 전체 관리(캡션·빼기·삭제)는 승인 후 `/life-timeline/[id]/edit`

#### `approveLocationWithPlaceAction` (신규 서버 액션)
- `draft-actions.ts` 에 추가
- `updateMany { id, userId, isDraft:true, subjectType:"location" }` 단일 가드 + coords 저장

#### `DraftReview.tsx` 업데이트
- 장소 행: `DraftRow` → `DraftLocationCard` 교체
- 이야기 행: border wrapper + `DraftRow` + `DraftPhotoUpload` 나란히
- `DraftRow.withBorder` prop 추가 (인물·물건은 자체 border, 이야기는 부모 처리)

---

### 음성 동반자 Step 8 — 세션 이어가기 (커버리지 요약 주입)

#### `fetchCoverageContext(userId)` 신규 (`lib/companion.ts`)
- DB 2쿼리 병렬:
  1. 승인된 `life_event` (isDraft=false, eventYear 있음) 최대 30개 — 연도+제목 나열
  2. 가장 최근 `CompanionSession` 의 미검토 draft (isDraft=true) 최대 10개
- LLM 요약 없음 (코드 나열, 비용 0). 항목 없으면 null = 첫 만남

#### `CompanionProfile` 확장
- `coverageSummary: string | null` 필드 추가
- `fetchCompanionProfile` 에서 `fetchCoverageContext` 병렬 추가
- `fetchCompanionProfile` 의 person 쿼리에 `isDraft: false` 추가 (draft 인물 프로파일 노출 방지)

#### `buildSystemPrompt` 확장
- coverageSummary 있으면 `[이미 나눈 이야기]` 섹션 주입
- coverageSummary=null 이면 섹션 생략 (첫 만남 동작 그대로)

#### 시스템 프롬프트 v3 (`COMPANION_SYSTEM_PROMPT_V3`)
- `[이어가기 — 이전에 나눈 이야기가 있을 때]` 섹션 추가
  - 오프닝에서 이미 들려주신 이야기를 따뜻하게 짚음
  - 다음 시기·빈 부분 자연스럽게 권유
  - 이미 다룬 주제는 먼저 다시 묻지 않음 (어르신 흐름 우선)
- `/api/companion` 라우트 무수정 (profile fetch 안에서 커버리지 자동 포함)

---

### 코드 자가감사 + H1·M2·M3 픽스

#### 감사 결과 요약

| # | 항목 | 결과 |
|---|---|---|
| 1 | 장소·물건 draft 레지스트리 노출 | ❌ H — 픽스 완료 |
| 2 | 세션 시간 상한 | ❌ M — 픽스 완료 |
| 3 | history slice 방향 | ❌ M — 픽스 완료 |
| 4 | NEXT_PUBLIC / 키 로그 | ✅ OK |
| 5 | Phase 분리 transcript+audio 보존 | ✅ OK |
| 6 | rec_ 정리 스크립트 | ✅ OK (영구 보존 의도됨) |

#### H1 — `listPeople`/`getPerson` isDraft 필터 누락 (`lib/people.ts`)
- `where: { userId }` → `where: { userId, isDraft: false }`
- 장소·물건·인물 draft 가 `/people` 레지스트리 탭에 즉시 노출되던 버그 수정

#### M2 — 세션 시간 상한 없음 (`CompanionClient.tsx`)
- `SESSION_MAX_MS = 40분` 타이머 추가 (오프닝 완료 직후 `startSessionTimer()` 가동)
- `MAX_TURNS_CLIENT = 50턴` 클라 턴 캡 추가
- 도달 시 amber 배너 ("오늘 정말 많은 이야기 해주셨어요!") + 저장 유도

#### M3 — history slice 방향 반대 (`app/api/companion/route.ts`)
- `slice(0, MAX_HISTORY_ITEMS)` = 오래된 앞부분 보존 → 최신 대화 잘림 버그
- `slice(-MAX_HISTORY_ITEMS)` 로 교체 = 최신 100개 보존

---

### 세션 상한 40분 → 30분 (`CompanionClient.tsx`)
- `SESSION_MAX_MS = 40 * 60 * 1000` → `30 * 60 * 1000`
- 배너 문구 "40분이 됐어요." → "30분이 됐어요."

---

---

### 채팅 온보딩 Step D — 거주지·학교 장소 매핑

#### 마이그레이션 1건
- `prisma/migrations/20260622000003_life_profile_place_coords`
- `LifeProfile` 에 `residencePlaces JSONB`, `schoolPlaces JSONB` nullable 추가
- 소비처: 미구현 (나중 map/poster용). `UserMemory.lat/lng` 는 TimelineView 에서 소비(지도 링크)이지만 온보딩은 LifeProfile JSON 별도 저장 (일관성 분리)

#### `PlaceableMultiItemWidget` 신규 (`app/onboarding-chat/widgets.tsx`)
- `MultiItemWidget` 확장 — 항목마다 "📍 지도에서 찾기" 토글 버튼 추가
- 클릭 시 `PlaceSearchInput` 인라인 노출 (기존 DraftLocationCard 패턴 재사용, 새 API 0)
- 동시에 하나만 열림 (`expandedIdx` 단일 상태)
- 좌표 선택(`lat !== null`) 하면 자동 닫힘 + 칩에 📍 표시 + emerald 색상 전환
- `onSubmit(names, places, qKey)` — 병렬 배열

#### `OnboardingChatClient.tsx` 변경
- `handleDirectSubmitWithPlaces(names, places, key)` 추가 — 좌표 있는 항목만 `residencePlaces`/`schoolPlaces` 키로 answersRef 에 저장
- `renderWidget()` 의 `textlist` 분기: key 가 `residences`|`schools` 이면 `PlaceableMultiItemWidget`, 나머지는 기존 `MultiItemWidget`

#### `actions.ts` 변경
- `ParsedAnswers` 에 `residencePlaces`/`schoolPlaces?: PlaceInfo[]` 추가
- `completeOnboardingChat` 에서 `hasCoord` 필터로 lat 있는 경우만 LifeProfile upsert

---

## 커밋 목록

| 커밋 해시 | 내용 |
|---|---|
| 6235948 | feat(companion): Step 7 — 지도 장소 지정 + 사진 첨부 (마이그 1) |
| a119940 | feat(companion): Step 8 — 세션 이어가기 (커버리지 요약 + 프롬프트 v3) |
| 2931398 | fix(companion): 자가감사 H1/M2/M3 픽스 |
| 7dde10b | fix(companion): 세션 상한 40분 → 30분 |

---

## 미완료 후속

- 음성 동반자 UX 검토: 40분 배너 + 턴 캡 배너 실사용 톤 확인 (어르신 대상)
- `/people` 장소·물건 탭 UX — 현재 탭 있으나 비어있을 때 empty state 없음
- `listEventsByPerson` 이 `monthEventId: null` 하드코딩 — 정책 변경 시 같이 봐야
- 동반자 세션에서 승인된 이야기 커버리지 텔레메트리 — 어떤 인생 단계가 먼저 채워지는지
- `PeopleConnectModal` 에서 draft 인물도 선택 가능한지 점검 (H1 픽스 이후 영향 범위)

---

## 검증

- `tsc --noEmit` 오류 0
- `npx prisma migrate deploy` 완료 (Supabase 운영 DB 반영)
